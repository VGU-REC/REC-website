// import {
//   Achievement,
//   Workshop,
//   RecEvent,
//   Blog,
//   ProjectIdea,
//   RecruitingProject,
//   PastProject,
//   User,
// } from "../../types";

// const parseAchievement = (object: Achievement) => {
//   const {title, imagePath, date} = object;
//   return [title, imagePath, date];
// };

// const parseWorkshop = (object: Workshop) => {
//   const {title, description, imagePaths, date} = object;
//   return [title, description, imagePaths, date];
// };
// const parseRecEvent = (object: RecEvent) => {
//   return [object.title,  object.description, object.imagePaths, object.date];
// };
// const parseProjectIdea = (object: ProjectIdea) => {
//   return [object.topic,  object.description];
// };
// // const parseRecruiting